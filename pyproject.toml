[project]
name = "aithos-selection"
version = "0.1.0"
description = "Curated knowledge base of AI primitives"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "typer>=0.12",
    "rich>=13.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pre-commit>=3.7",
]

[tool.uv]
package = false
