# TASKS — {{PROJECT_NAME}}

> Derivato dal PRD. Ogni task: piccolo, una iterazione, con verifica eseguibile.
> Formato: `- [ ] T-NN — descrizione | verify: <comando>`

- [ ] T-01 — esempio: setup struttura progetto | verify: `uv run python -c "import src"`
- [ ] T-NN (ultimo) — smoke-test end-to-end su servizi REALI (API/DB veri, non mock) prima di dichiarare done | verify: `<comando che colpisce il servizio reale>`
