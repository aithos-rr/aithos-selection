# Tasks

This directory holds task specifications for one-off phases of work on
this repository. Each task is a self-contained spec that an agent
(typically Claude Code) can follow autonomously.

During the bootstrap (Phases 1 through 5.5), this directory contained
the specs for each construction phase. Those are now in `archive/` for
historical reference.

## Active tasks

(None — bootstrap is complete.)

## Archived tasks

See [`archive/`](./archive/) for the completed bootstrap phases
(`phase-1-foundation.md` through `phase-5.5-coherence-check.md`).
